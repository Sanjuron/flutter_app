import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp( // MaterialApp est une sorte de wrapper pour le reste de l'app
      home: Text('Salut!'), // on sépare les propriétés par des virgules // sur la home screen se trouve un texte

  ));
}